services:
    app:
        build: .
        ports:
            - "8000:8000"
        env_file: .env 
        environment: 
            - REDIS_URL=redis://redis:6379
        depends_on:
            - redis

    redis:
        image: redis:alpine
        ports:
            - "6379:6379"
